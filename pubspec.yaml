name: puredent_app
description: PureDent - Dental clinic booking app
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # google_fonts: ^6.2.1  # optional: add back + `flutter pub get` (needs
  #                       # internet) if you want the exact Plus Jakarta
  #                       # Sans + Inter fonts from the HTML mockups.
  #                       # See the note in app_text_styles.dart.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
